diff --git a/packages/client/ui-commands/src/client/contract.ts b/packages/client/ui-commands/src/client/contract.ts
index 4f43747..66ad891 100644
--- a/packages/client/ui-commands/src/client/contract.ts
+++ b/packages/client/ui-commands/src/client/contract.ts
@@ -31,12 +31,34 @@ export interface SelectOption {
  * The shell component is owned by ui-commands; business never sees it. Both
  * callbacks receive the ClientSessionContext captured at popup open.
  */
-export type CommandUiSpec = {
+export interface PopupSelectSpec {
   readonly kind: 'popupSelect'
   options(session: ClientSessionContext, signal: AbortSignal): Promise<readonly SelectOption[]>
   onSelect(option: SelectOption, session: ClientSessionContext): void | Promise<void>
 }
 
+/** Context handed to a session-bound client action. */
+export interface ClientActionContext {
+  readonly session: ClientSessionContext
+  readonly signal: AbortSignal
+  restoreComposerFocus(): void
+}
+
+/** A client-owned asynchronous action with no popupSelect or Host command run. */
+export interface ClientActionSpec {
+  readonly kind: 'clientAction'
+  readonly busy?: 'ignore'
+  run(context: ClientActionContext): void | Promise<void>
+}
+
+/** Explicit runtime feature detection for business packages. */
+export interface CommandUiCapabilities {
+  readonly clientAction: true
+}
+
+/** The client behavior contributed by a command row. */
+export type CommandUiSpec = PopupSelectSpec | ClientActionSpec
+
 /**
  * One client-owned command contribution: a slash-menu entry whose behavior
  * lives entirely on the client (no host descriptor). Merged with the host
@@ -74,6 +96,8 @@ export interface CommandDecoration {
 
 /** The `ctx.commandUi` service face visible to business packages. */
 export interface CommandUiContract {
+  /** Runtime features this command surface implements. */
+  readonly capabilities: CommandUiCapabilities
   /**
    * Register one client command contribution; effect disposer. Duplicate
    * names throw at registration.
@@ -86,4 +110,6 @@ export interface CommandUiContract {
   decorate(decoration: CommandDecoration): () => void
   /** Resolve the per-session popup controller for one session scope (wiring/overlay layer). */
   popupFor(actx: ClientContext): unknown
+  /** Bind the real composer for one session; the conversation layer owns the focus implementation. */
+  bindComposerFocus(id: ClientSessionContext['sessionId'], focus: () => void): () => void
 }
diff --git a/packages/client/ui-commands/src/client/index.ts b/packages/client/ui-commands/src/client/index.ts
index 26e8a32..68a6881 100644
--- a/packages/client/ui-commands/src/client/index.ts
+++ b/packages/client/ui-commands/src/client/index.ts
@@ -27,7 +27,8 @@ export { filterOptions, PopupSelectController } from './popup.ts'
 export type { PopupSelectDeps, PopupSpec, PopupState, TokenSegment } from './popup.ts'
 export type { PopupSelectInjected, PopupSelectViewProps } from './PopupSelectView.tsx'
 export type {
-  CommandContribution, CommandDecoration, CommandUiContract, CommandUiSpec, SelectConfirmation, SelectOption,
+  ClientActionContext, ClientActionSpec, CommandContribution, CommandDecoration, CommandUiCapabilities, CommandUiContract, CommandUiSpec,
+  PopupSelectSpec, SelectConfirmation, SelectOption,
 } from './contract.ts'
 export type { CommandKey } from './locales.ts'
 
diff --git a/packages/client/ui-commands/src/client/service.ts b/packages/client/ui-commands/src/client/service.ts
index 4a606fe..b95fd41 100644
--- a/packages/client/ui-commands/src/client/service.ts
+++ b/packages/client/ui-commands/src/client/service.ts
@@ -21,7 +21,9 @@ import type {
   CandidateRequest, ClientSessionContext, CommandClaim, PickOutcome, InputTriggerCandidate, InputTriggerPick,
   SubmitEnvelope, SubmitImageAttachment, SubmitOutcome,
 } from '@deepseek-ai/dsh-client-ui-input-trigger/client'
-import type { CommandContribution, CommandDecoration, CommandUiContract } from './contract.ts'
+import type {
+  ClientActionContext, ClientActionSpec, CommandContribution, CommandDecoration, CommandUiContract, PopupSelectSpec,
+} from './contract.ts'
 import type { CommandDescriptor } from './directory.ts'
 import { CommandDirectory } from './directory.ts'
 import { PopupSelectController } from './popup.ts'
@@ -50,10 +52,18 @@ function submittedCommandName(line: string): string {
 }
 
 /** Live mutable state in one holder (service methods run behind the caller-ctx tracker). */
+interface ActiveClientAction {
+  readonly abort: AbortController
+  focusRestored: boolean
+  focusEligible: boolean
+  dispose: (() => void) | undefined
+}
+
 interface LiveState {
   readonly contributions: Map<string, CommandContribution>
   readonly decorations: Map<string, CommandDecoration>
   readonly popups: Map<SessionId, PopupSelectController<ClientSessionContext>>
+  readonly actions: Map<SessionId, ActiveClientAction>
 }
 
 /** One fuzzy match with its stable source position. */
@@ -124,7 +134,10 @@ export class CommandUiRuntime extends Service implements CommandUiContract {
   static inject = ['inputTriggers', 'sessions', 'remote', 'remote.commands']
 
   private readonly directory: CommandDirectory
-  private readonly live: LiveState = { contributions: new Map(), decorations: new Map(), popups: new Map() }
+  readonly capabilities = { clientAction: true } as const
+  private readonly live: LiveState = {
+    contributions: new Map(), decorations: new Map(), popups: new Map(), actions: new Map(),
+  }
   /** `command`-namespace translator (composer refusal notices). */
   private readonly t: TranslateNS<'command'>
 
@@ -160,6 +173,9 @@ export class CommandUiRuntime extends Service implements CommandUiContract {
     // prewarming so a newly opened menu waits for the replacement catalog.
     ctx.remote.$on('agent-preset/selected', (sessionId) => { this.directory.resetSession(sessionId) })
     ctx.on('connection/reset', () => { this.directory.resetConnected() })
+    ctx.effect(() => () => {
+      for (const id of [...this.live.actions.keys()]) this.abortClientAction(id)
+    }, 'command: client actions')
   }
 
   /**
@@ -225,6 +241,7 @@ export class CommandUiRuntime extends Service implements CommandUiContract {
     actx.effect(() => () => {
       controller.dispose()
       popups.delete(id)
+      this.abortClientAction(id)
       this.focusHooks.delete(id)
     }, 'command: session popup')
     return controller
@@ -273,7 +290,9 @@ export class CommandUiRuntime extends Service implements CommandUiContract {
     const name = pick.candidate.name
     const contribution = this.live.contributions.get(name)
     if (contribution !== undefined && contribution.available(pick.session)) {
-      this.openPopup(name, contribution.ui, pick.session, { via: 'menu', span: pick.span })
+      const segment = { via: 'menu', span: pick.span } as const
+      if (contribution.ui.kind === 'clientAction') this.startClientAction(name, contribution.ui, pick.session, segment)
+      else this.openPopup(name, contribution.ui, pick.session, segment)
       return 'handled'
     }
     const desc = this.directory.resolve(pick.session.sessionId, name)
@@ -283,7 +302,9 @@ export class CommandUiRuntime extends Service implements CommandUiContract {
     // manufactures one, and never touches the argument claim below.
     const decoration = this.live.decorations.get(name)
     if (decoration !== undefined && decoration.available(pick.session)) {
-      this.openPopup(name, decoration.ui, pick.session, { via: 'menu', span: pick.span })
+      const segment = { via: 'menu', span: pick.span } as const
+      if (decoration.ui.kind === 'clientAction') this.startClientAction(name, decoration.ui, pick.session, segment)
+      else this.openPopup(name, decoration.ui, pick.session, segment)
       return 'handled'
     }
     if (desc.input !== undefined) return { claim: this.leadingClaim(desc, pick.session) }
@@ -336,7 +357,9 @@ export class CommandUiRuntime extends Service implements CommandUiContract {
     if (contribution !== undefined && contribution.available(session)) {
       if (!bare) return undefined
       if (envelope.images > 0) refuseImages()
-      this.openPopup(name, contribution.ui, session, { via: 'enter', token })
+      const segment = { via: 'enter', token } as const
+      if (contribution.ui.kind === 'clientAction') this.startClientAction(name, contribution.ui, session, segment)
+      else this.openPopup(name, contribution.ui, session, segment)
       return 'handled'
     }
     await this.directory.ensureReady(session.sessionId, signal)
@@ -348,7 +371,9 @@ export class CommandUiRuntime extends Service implements CommandUiContract {
       const decoration = this.live.decorations.get(name)
       if (decoration !== undefined && decoration.available(session)) {
         if (envelope.images > 0) refuseImages()
-        this.openPopup(name, decoration.ui, session, { via: 'enter', token })
+        const segment = { via: 'enter', token } as const
+        if (decoration.ui.kind === 'clientAction') this.startClientAction(name, decoration.ui, session, segment)
+        else this.openPopup(name, decoration.ui, session, segment)
         return 'handled'
       }
     }
@@ -366,15 +391,91 @@ export class CommandUiRuntime extends Service implements CommandUiContract {
   /** Open the session's popup for one contribution or decoration (menu pick / bare enter). */
   private openPopup(
     name: string,
-    ui: CommandContribution['ui'],
+    ui: PopupSelectSpec,
     session: ClientSessionContext,
     segment: TokenSegment,
   ): void {
+    this.invalidateClientActionFocus(session.sessionId)
     const actx = this.scopeFor(session.sessionId)
     if (actx === undefined) return
     this.popupFor(actx).open(name, ui, session, segment)
   }
 
+  /** Start a session-owned client action without invoking the Host command executor. */
+  private startClientAction(
+    name: string,
+    spec: ClientActionSpec,
+    session: ClientSessionContext,
+    segment: TokenSegment,
+  ): void {
+    const id = session.sessionId
+    if (this.live.actions.has(id)) return
+    const actx = this.scopeFor(id)
+    if (actx === undefined) return
+    this.consumeVia(id, segment)
+    const active: ActiveClientAction = {
+      abort: new AbortController(),
+      focusRestored: false,
+      focusEligible: true,
+      dispose: undefined,
+    }
+    this.live.actions.set(id, active)
+    const dispose = actx.effect(() => () => this.clearOrAbortClientAction(id, active), 'command: session client action')
+    active.dispose = () => { void dispose() }
+    const context: ClientActionContext = {
+      session,
+      signal: active.abort.signal,
+      restoreComposerFocus: () => {
+        if (this.live.actions.get(id) !== active || active.abort.signal.aborted || !active.focusEligible || active.focusRestored) return
+        active.focusRestored = true
+        this.focusHooks.get(id)?.()
+      },
+    }
+    let completion: Promise<void>
+    try {
+      completion = Promise.resolve(spec.run(context))
+    } catch (error) {
+      completion = Promise.reject(error)
+    }
+    void completion.then(
+      () => this.clearClientAction(id, active),
+      (error: unknown) => {
+        if (!active.abort.signal.aborted) {
+          this.noticeFor(id, 'error', error instanceof Error ? error.message : String(error || `/${name} failed`))
+        }
+        this.clearClientAction(id, active)
+      },
+    )
+  }
+
+  private clearClientAction(id: SessionId, active: ActiveClientAction): void {
+    if (this.live.actions.get(id) !== active) return
+    this.live.actions.delete(id)
+    const dispose = active.dispose
+    active.dispose = undefined
+    dispose?.()
+  }
+
+  private clearOrAbortClientAction(id: SessionId, active: ActiveClientAction): void {
+    if (this.live.actions.get(id) !== active) return
+    active.abort.abort()
+    this.live.actions.delete(id)
+    const dispose = active.dispose
+    active.dispose = undefined
+    dispose?.()
+  }
+
+  /** A newer popup owns the next focus transition; an older action may still settle silently. */
+  private invalidateClientActionFocus(id: SessionId): void {
+    const active = this.live.actions.get(id)
+    if (active !== undefined) active.focusEligible = false
+  }
+
+  private abortClientAction(id: SessionId): void {
+    const active = this.live.actions.get(id)
+    if (active !== undefined) this.clearOrAbortClientAction(id, active)
+  }
+
   /** Build the leadingInput claim: token `/name ` + the command.execute submit transaction. */
   private leadingClaim(desc: CommandDescriptor, session: ClientSessionContext): CommandClaim {
     const token = `/${desc.name} `
diff --git a/packages/client/ui-commands/tests/service.client.spec.ts b/packages/client/ui-commands/tests/service.client.spec.ts
index ae8ef7a..3a35582 100644
--- a/packages/client/ui-commands/tests/service.client.spec.ts
+++ b/packages/client/ui-commands/tests/service.client.spec.ts
@@ -14,7 +14,7 @@ import { createScope, scopeOf } from '@deepseek-ai/dsh-api-session-controller/cl
 import type { SessionId } from '@deepseek-ai/dsh-session/types'
 import { RemoteError, TestRemote } from '@deepseek-ai/dsh-client-test-runtime'
 import type { ClientSessionContext, ConsumeTokenRequest, InputTriggerPick, InputTriggerSource, SubmitImageAttachment } from '@deepseek-ai/dsh-client-ui-input-trigger/client'
-import type { CommandContribution, CommandDecoration, CommandUiSpec, SelectOption } from '../src/client/contract.ts'
+import type { ClientActionSpec, CommandContribution, CommandDecoration, PopupSelectSpec, SelectOption } from '../src/client/contract.ts'
 import type { CommandDescriptor } from '../src/client/directory.ts'
 import { CommandUiRuntime } from '../src/client/service.ts'
 
@@ -155,7 +155,7 @@ function menuPick(source: InputTriggerSource, name: string, session: ClientSessi
   return source.onPick(pick)
 }
 
-const themeUi = (over: Partial<CommandUiSpec> = {}): CommandUiSpec => ({
+const themeUi = (over: Partial<PopupSelectSpec> = {}): PopupSelectSpec => ({
   kind: 'popupSelect',
   options: () => Promise.resolve([{ id: 'dark', label: 'Dark' }]),
   onSelect: () => undefined,
@@ -170,6 +170,15 @@ const themeContribution = (over: Partial<CommandContribution> = {}): CommandCont
   ...over,
 })
 
+const actionUi = (run: ClientActionSpec['run']): ClientActionSpec => ({ kind: 'clientAction', run })
+
+const actionContribution = (run: ClientActionSpec['run'], name = 'client-action'): CommandContribution => ({
+  name,
+  description: 'client action kind',
+  available: () => true,
+  ui: actionUi(run),
+})
+
 const req = (query: string, position: 'leading' | 'inline' = 'leading') =>
   ({ query, position, drilled: false, signal: new AbortController().signal })
 
@@ -390,6 +399,101 @@ describe('dispatch (menu column)', () => {
   })
 })
 
+describe('clientAction', () => {
+  it('uses one client action path for menu and bare Enter, consumes tokens, and never executes a Host command', async () => {
+    const { command, source, mint, executeCalls } = await bench()
+    const scope = mint('s1')
+    expect(command.capabilities).toEqual({ clientAction: true })
+    const consumes: ConsumeTokenRequest[] = []
+    scope.ctx.on('slash/input-consume-token', (request) => {
+      consumes.push(request)
+      return true
+    })
+    const calls: ClientSessionContext[] = []
+    command.register(actionContribution(({ session }) => { calls.push(session) }, 'act'))
+    expect(menuPick(source, 'act', proj('s1'), 4)).toBe('handled')
+    await expect(source.matchEnter!(proj('s1'), '/act', new AbortController().signal, { images: 0 })).resolves.toBe('handled')
+    await Promise.resolve()
+    expect(calls).toEqual([proj('s1')])
+    expect(consumes).toEqual([{ guard: { kind: 'span', span: { start: 0, end: 4, draftRev: 3 } } }])
+    expect(executeCalls).toEqual([])
+  })
+
+  it('ignores same-session re-entry until the active client action settles', async () => {
+    const { command, source, mint } = await bench()
+    mint('s1')
+    let release!: () => void
+    const run = vi.fn(() => new Promise<void>(resolve => { release = resolve }))
+    command.register(actionContribution(run, 'act'))
+    expect(menuPick(source, 'act', proj('s1'))).toBe('handled')
+    expect(menuPick(source, 'act', proj('s1'))).toBe('handled')
+    await Promise.resolve()
+    expect(run).toHaveBeenCalledOnce()
+    release()
+    await Promise.resolve()
+    await Promise.resolve()
+    expect(menuPick(source, 'act', proj('s1'))).toBe('handled')
+    await vi.waitFor(() => { expect(run).toHaveBeenCalledTimes(2) })
+  })
+
+  it('aborts an active client action when its session scope disposes', async () => {
+    const { command, source, mint } = await bench()
+    const scope = mint('s1')
+    let signal: AbortSignal | undefined
+    command.register(actionContribution(({ signal: received }) => {
+      signal = received
+      return new Promise<void>(() => {})
+    }, 'act'))
+    expect(menuPick(source, 'act', proj('s1'))).toBe('handled')
+    await Promise.resolve()
+    expect(signal?.aborted).toBe(false)
+    await scope.fiber.dispose()
+    expect(signal?.aborted).toBe(true)
+  })
+
+  it('notices non-abort action failures and permits an action-owned guarded focus restoration', async () => {
+    const { command, source, mint, notices } = await bench()
+    const scope = mint('s1')
+    const focus = vi.fn()
+    command.bindComposerFocus(sid('s1'), focus)
+    command.register(actionContribution(({ restoreComposerFocus }) => {
+      restoreComposerFocus()
+      restoreComposerFocus()
+      return Promise.reject(new Error('action failed'))
+    }, 'act'))
+    expect(menuPick(source, 'act', proj('s1'))).toBe('handled')
+    await vi.waitFor(() => {
+      expect(focus).toHaveBeenCalledOnce()
+      expect(notices).toEqual([{ scope: sid('s1'), level: 'error', text: 'action failed' }])
+    })
+    await scope.fiber.dispose()
+  })
+
+  it('does not let an older action reclaim composer focus after a later popup opens', async () => {
+    const { command, source, mint } = await bench()
+    const scope = mint('s1')
+    const focus = vi.fn()
+    command.bindComposerFocus(sid('s1'), focus)
+    let restoreComposerFocus: (() => void) | undefined
+    let settle: (() => void) | undefined
+    command.register(actionContribution(({ restoreComposerFocus: restore }) => new Promise<void>(resolve => {
+      restoreComposerFocus = restore
+      settle = resolve
+    }), 'act'))
+    command.register(themeContribution())
+
+    expect(menuPick(source, 'act', proj('s1'))).toBe('handled')
+    await vi.waitFor(() => { expect(restoreComposerFocus).toBeTypeOf('function') })
+    expect(menuPick(source, 'theme', proj('s1'))).toBe('handled')
+    restoreComposerFocus?.()
+    expect(focus).not.toHaveBeenCalled()
+
+    settle?.()
+    await Promise.resolve()
+    await scope.fiber.dispose()
+  })
+})
+
 describe('matchSpace (space column)', () => {
   it('answers undefined from a not-ready key (no waiting, no RPC)', async () => {
     const { source, listCalls } = await bench()
diff --git a/packages/client/ui-conversation/src/client/contract/input.ts b/packages/client/ui-conversation/src/client/contract/input.ts
index d6a0741..3eee336 100644
--- a/packages/client/ui-conversation/src/client/contract/input.ts
+++ b/packages/client/ui-conversation/src/client/contract/input.ts
@@ -274,6 +274,8 @@ export interface ComposerKeyboard {
   space(): boolean
   /** Dismiss the popupSelect shell (any interaction outside the box). */
   dismissPopup(): void
+  /** Bind the mounted Lexical composer to command-runtime focus restoration. */
+  bindComposerFocus(focus: () => void): () => void
 }
 
 /** One independently addressable row projected from the transient queue snapshot. */
diff --git a/packages/client/ui-conversation/src/client/input/facade.ts b/packages/client/ui-conversation/src/client/input/facade.ts
index e2fb81f..fd81171 100644
--- a/packages/client/ui-conversation/src/client/input/facade.ts
+++ b/packages/client/ui-conversation/src/client/input/facade.ts
@@ -60,6 +60,8 @@ export interface SessionInputDeps {
    * order (the empty-draft accelerated-Enter gesture); absent = unsupported.
    */
   steerQueue?: (() => void) | undefined
+  /** Bind the mounted composer to the command runtime for this session. */
+  bindComposerFocus?: ((focus: () => void) => () => void) | undefined
   /** The plain-message sink (send choreography / materialize fork — the hub owns it). */
   defaultSink(
     text: string,
@@ -433,6 +435,11 @@ export class SessionInputShell implements SessionInput {
     this.deps.popup?.()?.dismiss()
   }
 
+  /** Register the real mounted composer focus callback with the command runtime. */
+  bindComposerFocus(focus: () => void): () => void {
+    return this.deps.bindComposerFocus?.(focus) ?? (() => {})
+  }
+
   /**
    * The live selection as a detect-coordinate span (menu-launcher synthetic
    * hits replace it on pick); an absent selection answers a collapsed span at
diff --git a/packages/client/ui-conversation/src/client/input/hub.ts b/packages/client/ui-conversation/src/client/input/hub.ts
index 6c71324..99a35b4 100644
--- a/packages/client/ui-conversation/src/client/input/hub.ts
+++ b/packages/client/ui-conversation/src/client/input/hub.ts
@@ -25,6 +25,7 @@ import { SessionInputShell } from './facade.ts'
 /** Structural command face for per-session popup resolution. */
 interface CommandFace {
   popupFor(actx: Context): PopupDismissFace
+  bindComposerFocus(id: SessionId, focus: () => void): () => void
 }
 
 /** Optional input-trigger service resolved without importing its implementation. */
@@ -90,6 +91,7 @@ export class InputHub implements SessionInputResolver {
       queue: queueReadFaceOf(session),
       defaultSink: (text, imageIds, mode, signal) => this.sink(session, text, imageIds, mode, signal),
       steerQueue: () => { void this.steerQueue(session, shell) },
+      bindComposerFocus: focus => this.bindComposerFocus(id, focus),
       commandImages: {
         serialize: ids => this.conversation().serializeDraftImages(ids),
         // Asymmetric with serialize on purpose: release settles AFTER the
@@ -217,6 +219,11 @@ export class InputHub implements SessionInputResolver {
     return command?.popupFor(actx)
   }
 
+  private bindComposerFocus(id: SessionId, focus: () => void): () => void {
+    const command = this.rootCtx.get('commandUi') as CommandFace | undefined
+    return command?.bindComposerFocus(id, focus) ?? (() => {})
+  }
+
   private sessions(): ISessions {
     const sessions = this.rootCtx.get('sessions')
     if (sessions === undefined) throw new Error('conversation.input: sessions service unavailable')
diff --git a/packages/client/ui-conversation/src/client/skeleton/InputBar.tsx b/packages/client/ui-conversation/src/client/skeleton/InputBar.tsx
index 6c8c46c..3f0b4d3 100644
--- a/packages/client/ui-conversation/src/client/skeleton/InputBar.tsx
+++ b/packages/client/ui-conversation/src/client/skeleton/InputBar.tsx
@@ -166,6 +166,14 @@ export function InputBar({
     else if (rect.top < box.top) scrollEl.scrollTop -= box.top - rect.top
   }
 
+  useEffect(() => {
+    if (sessionId === undefined || editor === null || keyboard === undefined) return
+    return keyboard.bindComposerFocus(() => {
+      editor.getRootElement()?.focus({ preventScroll: true })
+      editor.focus(() => { revealSelection() })
+    })
+  }, [editor, keyboard, sessionId])
+
   // Unlock (mount / session switch) returns focus to the box, and owns the
   // reveal that comes with it. Lexical's focus() suppresses the browser's
   // scroll walk (preventScroll inside), so the reveal in our own scrollport
diff --git a/packages/client/ui-model-selection/tests/browser-plugin.client.spec.ts b/packages/client/ui-model-selection/tests/browser-plugin.client.spec.ts
index 1bf1173..c28abe9 100644
--- a/packages/client/ui-model-selection/tests/browser-plugin.client.spec.ts
+++ b/packages/client/ui-model-selection/tests/browser-plugin.client.spec.ts
@@ -20,6 +20,7 @@ import type { CommandContribution, SelectOption } from '@deepseek-ai/dsh-client-
 import type { ModelSelectInjected } from '../src/client/slots.ts'
 import { apply, inject } from '../src/client/index.ts'
 import { zh } from '../src/client/locales.ts'
+import type { PopupSelectSpec } from '@deepseek-ai/dsh-client-ui-commands/client'
 
 const sid = (k: string): SessionId => k as SessionId
 
@@ -182,7 +183,7 @@ describe('ui-model-selection dual entry', () => {
   it('popup options mark the host current active with the provider group in the detail', async () => {
     const b = await bench()
     b.mint('s1')
-    const options = await b.contribution().ui.options(projection('s1'), new AbortController().signal)
+    const options = await (b.contribution().ui as PopupSelectSpec).options(projection('s1'), new AbortController().signal)
     expect(options.map((o: SelectOption) => o.label)).toEqual(['DeepSeek-V4-Flash', 'DeepSeek-V4-Pro'])
     expect(options[0]).toMatchObject({ active: true, detail: 'DeepSeek' })
     expect(options[1]?.active).toBeUndefined()
@@ -209,7 +210,7 @@ describe('ui-model-selection dual entry', () => {
       reasoningEffort: 'max',
     })
     // The POPUP's next options pass reflects it without a seat-side reload.
-    const options = await b.contribution().ui.options(projection('s1'), new AbortController().signal)
+    const options = await (b.contribution().ui as PopupSelectSpec).options(projection('s1'), new AbortController().signal)
     expect(options.find((o: SelectOption) => o.label === 'DeepSeek-V4-Pro')).toMatchObject({ active: true })
   })
 
@@ -217,9 +218,9 @@ describe('ui-model-selection dual entry', () => {
     const b = await bench()
     b.mint('s1')
     const seatFace = b.seat().inject!(sid('s1'))
-    const options = await b.contribution().ui.options(projection('s1'), new AbortController().signal)
+    const options = await (b.contribution().ui as PopupSelectSpec).options(projection('s1'), new AbortController().signal)
     const pro = options.find((o: SelectOption) => o.label === 'DeepSeek-V4-Pro')!
-    await b.contribution().ui.onSelect(pro, projection('s1'))
+    await (b.contribution().ui as PopupSelectSpec).onSelect(pro, projection('s1'))
     expect(seatFace.directory.getSnapshot().current).toEqual({
       provider: 'deepseek-official',
       model: 'deepseek-v4-pro',
@@ -239,8 +240,8 @@ describe('ui-model-selection dual entry', () => {
     // The service face resolves the same instance the seat inject handed out.
     expect(b.ctx.modelDirectories.directoryFor(sid('a')).store).toBe(faceA.directory)
     await Promise.all([
-      b.contribution().ui.options(projection('a'), new AbortController().signal),
-      b.contribution().ui.options(projection('b'), new AbortController().signal),
+      (b.contribution().ui as PopupSelectSpec).options(projection('a'), new AbortController().signal),
+      (b.contribution().ui as PopupSelectSpec).options(projection('b'), new AbortController().signal),
     ])
     expect(b.calls.models).toBe(1)
   })
@@ -369,7 +370,7 @@ describe('ui-model-selection dual entry', () => {
     b.address(sid('child'))
 
     expect(b.contribution().available(projection('child'))).toBe(false)
-    await expect(b.contribution().ui.options(
+    await expect((b.contribution().ui as PopupSelectSpec).options(
       projection('child'),
       new AbortController().signal,
     )).rejects.toThrow(/unavailable for addressed subagent/)
diff --git a/packages/client/ui-permission-presets/tests/browser-plugin.client.spec.ts b/packages/client/ui-permission-presets/tests/browser-plugin.client.spec.ts
index e6f9cd3..06dddd1 100644
--- a/packages/client/ui-permission-presets/tests/browser-plugin.client.spec.ts
+++ b/packages/client/ui-permission-presets/tests/browser-plugin.client.spec.ts
@@ -22,6 +22,7 @@ import {
 } from '../src/client/PermissionRow.tsx'
 import { apply, inject } from '../src/client/index.ts'
 import { accessEn, accessZh } from '../src/client/locales.ts'
+import type { PopupSelectSpec } from '@deepseek-ai/dsh-client-ui-commands/client'
 
 const sid = (k: string): SessionId => k as SessionId
 
@@ -110,11 +111,11 @@ describe('ui-permission browser plugin', () => {
     expect(c.available(proj)).toBe(false)
     b.values.set(sid('s1'), { ...SELECT, options: [...SELECT.options, { value: 'custom', name: 'Custom' }], currentValue: 'custom' })
     expect(c.available(proj)).toBe(true)
-    const options = await c.ui.options(proj, new AbortController().signal)
+    const options = await (c.ui as PopupSelectSpec).options(proj, new AbortController().signal)
     expect(options.map(option => option.id)).toEqual(['read-only', 'workspace-write', 'danger-full-access'])
     expect(options.every(option => option.active !== true)).toBe(true)
     b.values.set(sid('s1'), SELECT)
-    const again = await c.ui.options(proj, new AbortController().signal)
+    const again = await (c.ui as PopupSelectSpec).options(proj, new AbortController().signal)
     expect(again.find(option => option.id === 'workspace-write')?.active).toBe(true)
     expect(again.find(option => option.id === 'read-only')?.detail).toBe('Reads only.')
     // English built-ins use product labels; other kebab-case names title-case.
@@ -127,7 +128,7 @@ describe('ui-permission browser plugin', () => {
       confirmLabel: 'Enable Full access',
     })
     b.locale.setLocale('zh')
-    const localized = await c.ui.options(proj, new AbortController().signal)
+    const localized = await (c.ui as PopupSelectSpec).options(proj, new AbortController().signal)
     expect(localized.map(option => option.label)).toEqual(['仅可查看', '工作区内修改', '完全权限'])
     expect(localized.find(option => option.id === 'danger-full-access')?.confirmation).toEqual({
       title: '确认启用完全权限？',
@@ -143,12 +144,12 @@ describe('ui-permission browser plugin', () => {
       { value: '__proto__', name: '__proto__' },
       { value: 'plain', name: 'Ask Every Time' },
     ] })
-    const passthrough = await c.ui.options(proj, new AbortController().signal)
+    const passthrough = await (c.ui as PopupSelectSpec).options(proj, new AbortController().signal)
     expect(passthrough.map(option => option.label)).toEqual([
       'Project Files', 'Operator Mode', 'Custom Mode', '__proto__', 'Ask Every Time',
     ])
     // A projection that vanished between availability and open throws.
-    expect(() => c.ui.options({ sessionId: sid('ghost') }, new AbortController().signal))
+    expect(() => (c.ui as PopupSelectSpec).options({ sessionId: sid('ghost') }, new AbortController().signal))
       .toThrow(/not available on this host/)
   })
 
@@ -157,13 +158,13 @@ describe('ui-permission browser plugin', () => {
     const c = b.decoration()!
     const proj = { sessionId: sid('s1') }
     b.values.set(sid('s1'), SELECT)
-    await c.ui.onSelect({ id: 'danger-full-access', label: 'danger-full-access' }, proj)
+    await (c.ui as PopupSelectSpec).onSelect({ id: 'danger-full-access', label: 'danger-full-access' }, proj)
     expect(b.commands).toEqual(['/permission danger-full-access'])
     b.setResult({ ok: false })
-    await expect(c.ui.onSelect({ id: 'read-only', label: 'read-only' }, proj)).rejects.toThrow(/permission switch failed/)
+    await expect((c.ui as PopupSelectSpec).onSelect({ id: 'read-only', label: 'read-only' }, proj)).rejects.toThrow(/permission switch failed/)
     b.setResult({ ok: true, matched: false })
-    await expect(c.ui.onSelect({ id: 'read-only', label: 'read-only' }, proj)).rejects.toThrow(/no \/permission command/)
+    await expect((c.ui as PopupSelectSpec).onSelect({ id: 'read-only', label: 'read-only' }, proj)).rejects.toThrow(/no \/permission command/)
     // An unmaterialized session throws before any submit.
-    await expect(c.ui.onSelect({ id: 'read-only', label: 'read-only' }, { sessionId: sid('ghost') }))
+    await expect((c.ui as PopupSelectSpec).onSelect({ id: 'read-only', label: 'read-only' }, { sessionId: sid('ghost') }))
       .rejects.toThrow(/not materialized/)
   })
