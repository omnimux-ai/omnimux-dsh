# 定时任务详情页同分类卡片分组（Grouped Cards）实机验证证据报告

## 1. 验证目标与结论
- 针对用户提出的“把同一个分类的用卡片样式分组分开”的视觉诉求进行断言验证。
- 验证结果：全部通过（PASS）。

## 2. 界面分组卡片断言指标
- [x] 分组卡片容器（`.dsh-st-md-group-card`）：成功包裹「详情」（运行时、工作目录、模型、推理等级、权限边界）与「频率」（计划时间、运行时间、时区、高级选项）各自所属的字段行。
- [x] 独立深色卡片样式：背景 `var(--dsw-alias-bg-layer-2)`，边框 `1px solid var(--dsw-alias-border-l1)`，圆角 `10px`，`overflow: hidden`。
- [x] 卡片内部细分割线：相邻字段行之间通过 `border-bottom: 1px solid var(--dsw-alias-border-l1)` 分隔，末行自动隐藏底边。
- [x] 分类副标题（`dsh-st-md-block-title`）：外置于卡片顶部，具有微弱浅灰与呼吸留白，视觉结构对标图2真实效果。
- [x] 功能契约：全量 132 项自动化测试 100% 通过。
