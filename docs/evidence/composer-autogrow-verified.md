# 图像生成输入框高度

Issue #2575。空框和一行保持原来的矮高度（52px）。写到第 10 行长到顶（228px），10 行都露在外面，没有滚动条。第 11 行起不再长高，多出来的字在框里滚动，滚到底能看见最后一行。删回一行或清空后，框回到矮高度。窗口变宽变窄时按新的折行重算。

截图：`composer-autogrow-10-lines.png`（正好 10 行）、`composer-autogrow-overflow.png`（第 11 行起在框里滚动）。
