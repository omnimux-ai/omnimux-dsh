# 实机验证证据：消除左侧栏收起后的黑边空白死区

- 日期：2026-09-15
- 任务：修复收起左侧侧边栏后遗留 56-90px 黑边死区的问题，实现全屏铺满与无黑边展开。
- 截图证据：`docs/evidence/sidebar-collapsed-black-bar-fixed.png`
- CDP 实机量测：
  - 修复前：`frameGrid: 280px 1448px 0px`，`chatRect.left: 280px`（存在 280px 纯黑空白带）；
  - 修复后：`frameGrid: 0px 1728px 0px`，`chatRect.left: 0px`，`chatRect.width: 1728px`（完全撑满视口，黑边彻底消除为 0px）。

## 验证结果记录
1. **纯会话收起态**：首列网格强制为 0px，会话列从 x=0 撑满全部宽度（100vw），彻底根除左侧黑边；
2. **分屏收起态**：首列网格为 0px，会话列占 440px 紧靠视口左边缘，右侧工作台占满剩余视口宽度；
3. **展开恢复态**：点击展开后首列平滑恢复 280px，无样式冲突或视觉跳动。
