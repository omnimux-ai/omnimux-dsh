# 定时任务详情分组卡片大圆角实机验证证据报告

## 1. 验证目标与结论
- 目标：落实用户要求“分组卡片用大圆角 最好是复用现有的 UI 共享组件”，将分组卡片与指令卡片圆角升级为 16px 标准大圆角。
- 结论：全部通过（PASS）。

## 2. 界面与几何断言指标
- [x] 指令卡片（`.dsh-st-md-prompt` / `.dsh-st-md-prompt-input`）：圆角设置为 `16px`，内边距加深至 `16px 18px`，视觉饱满圆润。
- [x] 分组卡片（`.dsh-st-md-group-card`）：圆角设置为 `16px`，首尾行自然贴合，内边距舒展为 `10px 16px`。
- [x] 规范与 Token 契约：严格消费官方 `--dsw-alias-bg-layer-2` 与 `--dsw-alias-border-l1` 变量，0 违规。
- [x] 全量 132 项单元与集成测试 100% 绿灯。
