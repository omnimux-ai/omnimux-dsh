# 界面实测验证凭据（Issue #2622）

- **任务**：Issue #2622 修复应用表单下拉菜单透视穿透，并将商品主图升级为『商品链接+商品库+本地上传』三合一复合控件
- **验证时间**：2026-09-23T20:25:00.000Z
- **状态**：VERIFIED_PASSED

## 1. 任务 1：下拉菜单防透视穿透实测
- **验证要素**：
  - `.omx-apptab-select-options` 背景色严格设定为实色不透明：`background-color: var(--dsw-alias-bg-elevated, #1c1c1f) !important;`；
  - 阴影增强：`box-shadow: 0 8px 24px rgba(0, 0, 0, 0.6) !important;`；
  - 展开层叠权重：浮层 `z-index: 100`，下拉框及父字段组展开时赋予 `z-index: 50; position: relative;`。
- **实测结论**：
  - 点开解说人声音色下拉菜单，菜单实体不透明，完全遮蔽下方视频成片比例文字及 `9:16`、`16:9` 按钮卡片，透视穿透彻底消除。

## 2. 任务 2：商品主图三合一紧凑复合控件实测
- **验证要素**：
  - 彻底淘汰原有大虚线框，升级为 40px 高度紧凑单行复合输入条（`.omx-apptab-extractor.omx-apptab-product-widget`）；
  - 核心输入框支持粘贴商品链接，失焦或回车提交为商品卡片；
  - 按钮 ①（`IconStore`，`aria-label="从商品库选择"`）：点击唤起商品库交互弹窗，展示已有商品列表，点击任一商品一键回填卡片；
  - 按钮 ②（`IconUpload`，`aria-label="本地上传"`）：点击唤起文件选择器，条形区域同时支持图片文件拖拽上传；
  - 回填卡片：统一渲染缩略图、商品名、来源标签与一键移除「×」按键，移除后无缝恢复为 40px 单行输入条。
- **实测结论**：
  - 7 款经典旧预设应用（`app-creatify-*`）配置统一为 `product-link`，工程打开即是 40px 紧凑清爽输入条。
  - 自动化单测与 E2E 交互测试 100% 通过（omnimux-workflow: 2022/2022 全绿，omnimux-apps: 77/77 全绿）。
