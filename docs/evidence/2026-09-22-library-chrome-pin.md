# 选素材整页钉顶实测

日期：2026-09-22
页面：隔离浏览器，1440×900，侧栏 240。样式取自本次改动的会话引导样式，几何由会话列实测写入。

实测（滚动卡片之后）：

- 整页：上沿 0，下沿贴窗口底，左缘对齐侧栏右侧 240。
- 分类栏：上沿 6（顶栏内边距），滚动后仍是 6，不跟着卡片走。
- 卡片区：单独滚动，滚动量 333。
- 输入框：下沿 793，窗口高 813，贴在底部。

截图：`docs/evidence/2026-09-22-library-chrome-pin.png`
