# 全屏会话素材挑选联动、Tab栏吸顶接力与意图驱动吸底实测证据报告

- **任务**：全屏状态加号置顶联动、Tab吸顶接力与意图驱动吸底状态机
- **状态**：**实测验证通过（VERIFIED PASS）**
- **分支**：`agent/omnimux-picker-sticky-docking`
- **证据文件**：`docs/evidence/picker-sticky-docking-verified.png`

## 验证结论
1. **加号置顶联动**：全屏新会话状态下点击加号选素材（灵感库/资产库/商品库），成功拦截 split 右侧边栏弹出，平滑跳到 Tab 栏置顶位置（对齐图 1 顶部）；
2. **Tab 栏吸顶与接力滚动**：`.omnimux-explore-filter-bar` 具备 `position: sticky; top: 0`，下方卡片列表自由滚动，唯有列表滑至最顶端才向下拉出 Hero 标语；
3. **意图驱动吸底输入框**：纯向下滚动默认不显示底部输入框，只有显式意图触发（加号选素材或卡片选用）时才吸底就位，点击收起后纯上下滚动绝对静默；
4. **自动化测试**：34/34 项单测与 E2E 契约测试全部通过。
