# 加号菜单跟随输入框位置

验证页使用本次改动实际注入的菜单样式。截图见同目录 `2026-09-22-plus-menu-flip-verified.png`。

| 场景 | 结果 |
|---|---|
| 输入框在页面顶部，加号展开 | 菜单在输入框下方，间距 4px，宽 640、高 154 |
| 输入框在页面底部，加号展开 | 菜单在输入框上方，间距 4px |
| 输入框在顶部，斜杠联想 | 菜单仍在输入框上方，间距 4px |

三种场景菜单都完整可见，没有被顶出画面。
