# 资产库内产品库 Tab 布局 1:1 对齐原页面实测验证证据

## 1. 验证目标与规范闭环
- 对照原独立产品库（`商品与产品库`）页面，1:1 像素级还原全部交互与视觉细节；
- 消除原有布局偏差，彻底清退多余胶囊行与不匹配图标。

## 2. 实测数据与对比结论
1. **操作栏动作行**：
   - 处于产品库 Tab 时，主按钮升级为 `CreateProductMenu`：白色胶囊按钮带加号图标、文案「添加产品」、右侧小箭头 `ChevronDownIcon`，鼠标悬停 150ms 展开实物/数字产品分流菜单；
   - 次按钮对齐为 `[ 💬 对话中添加 ]`；
2. **选项卡与工具栏**：
   - 一级 Tab 保留 `本地` | `云端` | `产品库`；
   - 在产品库 Tab 下，移除右侧网格/列表切换小方块；
   - 产品库自身工具栏承载 `全部` / `实物产品` / `数字产品` 下划线 Tabs，右侧为拉伸长搜索框（占位符 `搜索产品名称、卖点、品牌`）；
3. **空状态 1:1 还原**：
   - 全宽居中 `.omnimux-products-empty` 容器，带 `1px dashed var(--dsw-alias-border-l4)` 虚线圆角边框；
   - 居中文案：`暂无产品数据。点击「添加产品」录入首件标品。`；
   - 下方居中展示带下拉小箭头的 `<CreateProductMenu>` 按钮；
4. **自动化验证与测试通过率**：
   - `omnimux-assets` 480/480 单测 100% 通过；
   - E2E 测试 100% 通过；
   - 客户端编译产物 `lib/client.js` 335,378 字节无报错。
