# 选素材整页：输入框必须留在窗口最底

Issue: #2573
新用户基线：不依赖本机私有服务。读不到素材时仍显示已有空态，输入框位置不变。

## 现象

分类已钉在窗口最上。输入框没有沉到底：它停在页面上半截，被整页盖住。右下角只剩「收起输入框」。

## 期望

1. 从加号打开商品库、资产库、灵感库后，输入框固定在窗口最底，能看见、能输入。
2. 分类仍贴在会话列最上，不盖住侧栏。
3. 卡片多时只滚中间。
4. 关闭整页后，输入框回到原来的位置。

## 验收

- 整页打开期间，输入框是贴底状态，不被整页盖住。
- 量宽度时不把贴底状态清掉。
- 关闭后贴底标记消失。
