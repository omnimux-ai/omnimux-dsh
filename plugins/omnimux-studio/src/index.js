/**
 * Host entry point for omnimux-studio plugin.
 * Strictly adheres to pure-frontend plugin architecture (UI frontend only, no backend).
 */

export const name = 'omnimux-studio'
export const inject = []

/**
 * Cordis host apply.
 *
 * @param {object} ctx
 */
export function apply(ctx) {
  // Pure-frontend plugin: no backend tools or HTTP routes needed.
}
