/**
 * videoParams/viewportPositioner — re-export（2026-09-07 全模态收敛 / T01）。
 * 实现已升格至 ../cfg/viewportPositioner.ts，本文件仅保持既有引用路径。
 */

export {
  GAP,
  PANEL_DEFAULT_MAX_HEIGHT,
  PANEL_MIN_HEIGHT,
  PANEL_WIDTH,
  VIEWPORT_PADDING,
  calculatePopoverPosition,
  resolvePanelWidth,
} from '../cfg/viewportPositioner.ts';
